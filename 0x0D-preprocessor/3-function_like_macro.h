#ifndef FUNCTION_LIKE_MACRO
#define FUNCTION_LIKE_MACRO
/**
  *3-function_like_macro.h - defines a macro that provides the absolute value
  *@x: expects an number
  */
#define ABS(x) ((x) < 0 ? (-x) : (x))
#endif
